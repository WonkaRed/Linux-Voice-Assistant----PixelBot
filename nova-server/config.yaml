server:
  host: "0.0.0.0"
  port: 9600
  auth_token: ""  # Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"

pixelbot:
  agent_name: "main"
  session_id: "nova-desktop"
  timeout: 30
  max_retries: 1
  retry_delay: 2.0

soul:
  directory: "soul"
