# Nova voice bridge — dependencies
# Install:  pip install -r requirements.txt

# Config
PyYAML>=6.0.0

# Telegram user-client relay to Hermes
Telethon>=1.36.0

# Speech-to-text (GPU-accelerated Whisper)
faster-whisper>=1.0.0
# Audio capture uses a subprocess recorder (arecord / parec / ffmpeg) — no
# PortAudio/PyAudio, so no system dev packages are required.

# Text-to-speech
piper-tts>=1.2.0
onnxruntime>=1.16.0

# Keyboard hotkey fallback (evdev); primary path is the Wayland socket
evdev>=1.6.0

# CUDA runtime libs for faster-whisper / CTranslate2 (bundled wheels)
nvidia-cudnn-cu12>=9.0.0
nvidia-cublas-cu12>=12.0.0
